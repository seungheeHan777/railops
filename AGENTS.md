# AGENTS.md

이 문서는 RailOps 프로젝트에서 Codex와 후속 에이전트가 작업할 때 반드시 먼저 읽어야 하는 작업 기준입니다.

## 프로젝트 요약

RailOps는 좌석 선택 기반 철도 예매 시스템입니다. 핵심 목표는 사용자가 특정 운행편의 객차와 좌석을 직접 선택하고, 결제 전 좌석 임시 점유와 만료 처리, 가상 결제, 중복 예매 방지까지 구현하는 것입니다.

RailOps라는 이름은 `Rail`과 `Operations`를 합친 말입니다. 단순 예매 기능뿐 아니라 배포, 로그, 모니터링, 장애 대응처럼 운영 관점까지 포함한다는 의미입니다.

## 현재 작업 단계

현재 단계는 초기 문서화와 저장소 구조 구성입니다.

아직 Spring Boot 또는 React 프로젝트 코드를 생성하지 않습니다. 먼저 문서, 도메인 모델, API 설계, 화면 범위, 운영 원칙을 정리한 뒤 백엔드/API 구현으로 넘어갑니다.

## 작업 순서

1. 문서와 요구사항 정리
2. 도메인 모델, ERD, API 명세 구체화
3. Spring Boot 기본 구조 생성
4. 인증/인가와 공통 응답/예외 처리 구현
5. 역, 노선, 열차, 운행 편성 관리 API 구현
6. 좌석 조회와 좌석 선택 API 구현
7. 좌석 HOLD, 예매, 가상 결제 구현
8. 동시성 제어 테스트 구현
9. Redis 캐시와 HOLD 만료 보조 처리 적용
10. 최소 React UI 구현
11. Docker Compose, Nginx, 배포 구성
12. 모니터링, 부하 테스트, 장애 대응 문서화

## 핵심 설계 원칙

- 좌석 예매 정합성이 최우선입니다.
- 좌석 상태는 AVAILABLE, HELD, RESERVED, BLOCKED로 분리합니다.
- HELD는 결제 전 임시 점유이며 만료 시간이 있습니다.
- BLOCKED는 관리자 또는 시스템이 판매를 막은 상태이며 자동 해제하지 않습니다.
- HOLD 만료의 1차 정합성 기준은 PostgreSQL의 `hold_expires_at`입니다.
- Spring Scheduler는 만료된 HOLD를 주기적으로 정리하는 역할을 합니다.
- 결제 성공 API에서도 `hold_expires_at` 만료 여부를 반드시 재검증합니다.
- Redis TTL은 보조 수단으로 사용할 수 있지만, 예매 정합성을 Redis에만 의존하지 않습니다.
- 동시성 제어는 1차 구현에서 ScheduleSeat 행에 대한 비관적 락을 우선 검토합니다.
- 여러 좌석을 동시에 점유할 때는 좌석 ID 오름차순으로 잠가 데드락 가능성을 줄입니다.

## 프론트엔드 범위

프론트엔드는 핵심이 아니지만 전체 흐름을 확인할 수 있는 최소 UI는 필요합니다.

필수 화면:

- 메인 페이지
- 회원가입
- 로그인
- 마이페이지
- 열차 검색
- 열차 상세
- 객차/좌석 선택
- 예매 확인
- 가상 결제
- 내 예매 목록
- 예매 상세
- 관리자 대시보드
- 역/노선/열차/운행 편성 관리
- 좌석 BLOCK / UNBLOCK
- 전체 예매 조회
- 운영 로그 조회

## 문서 작성 규칙

- 문서는 한국어로 작성합니다.
- 주요 결정은 README, docs, 또는 `docs/15-decision-log.md`에 기록합니다.
- 구현을 시작하기 전 설계 문서와 요구사항을 먼저 갱신합니다.
- 너무 짧은 메모가 아니라 포트폴리오에 사용할 수 있는 설명 가능한 문서로 작성합니다.
- 확정되지 않은 내용은 확정처럼 쓰지 말고 `검토 필요` 또는 `후순위`로 표시합니다.

## 구현 규칙

- 불필요하게 복잡한 아키텍처를 만들지 않습니다.
- 초기 운영은 단일 EC2 + Docker Compose 기준입니다.
- RDS, ElastiCache, 다중 WAS, Kubernetes, 무중단 배포는 후순위 확장 목표입니다.
- 백엔드는 Spring Boot 관례를 따릅니다.
- API는 REST 기준으로 설계합니다.
- 테스트는 좌석 HOLD, 결제 상태 전이, 동시성 제어처럼 위험도가 큰 흐름부터 작성합니다.
- 운영 로그는 예매/결제/좌석 차단/로그인 실패 같은 주요 이벤트 중심으로 남깁니다.

## 에이전트 작업 방식

작업 시작 시 다음을 확인합니다.

1. `AGENTS.md`
2. `TODO.md`
3. `README.md`
4. 관련 `docs/*.md`

작업 완료 시 다음을 갱신합니다.

- 변경된 설계 문서
- TODO 진행 상태
- 새로 결정된 사항이 있으면 decision log
- 구현이 있었다면 테스트 또는 검증 결과

사용자가 명시적으로 요청하지 않는 한, 현재 단계와 무관한 대규모 구현은 시작하지 않습니다.
